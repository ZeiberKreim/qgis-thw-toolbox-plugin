# THW Toolbox configuration.
# Edit this file and reload the plugin to apply changes.
# The environment variable THW_TOOLBOX_LOG_LEVEL overrides the `level` below.

[logging]
# Available levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG is verbose — only enable when hunting a bug.
level = INFO
